# Next.js + Payload v3 + Vercel + Neon (PostgreSQL)

# Required (server)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require
PAYLOAD_SECRET=replace-with-a-long-random-secret

# Public server URL for Payload (server-side origin resolution)
# Examples:
# - Local: http://localhost:3000
# - Vercel: https://your-project.vercel.app
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

# Optional: site URL used by frontend/server if you prefer this name
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: override origin used by server-side fetches (defaults to PAYLOAD_PUBLIC_SERVER_URL / VERCEL_URL)
NEXT_PUBLIC_PAYLOAD_URL=http://localhost:3000

# Optional: server-only token if your REST endpoints require auth
PAYLOAD_API_TOKEN=

# Optional: tuning (safe defaults are applied if unset)
PAYLOAD_REVALIDATE_SECONDS=60
PAYLOAD_FETCH_TIMEOUT_MS=2500

# Legacy (Vite) - kept only for reference
# VITE_PAYLOAD_API_URL=http://localhost:3000/api
# VITE_PAYLOAD_TOKEN=your_payload_token_here
